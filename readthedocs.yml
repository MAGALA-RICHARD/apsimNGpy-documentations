version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

sphinx:
  configuration: doc/conf.py

python:
  install:
    - pip
    - sphinx
    - sphinx_rtd_theme
    - myst_parser
    - sphinx-copybutton
    - sphinxcontrib-mermaid
